package Days16;

public class Employee {
	
	int salary;
	
	}
/* Java source code file. java 
 * When program is not running , which is the method 
 * in the above, it will be stored to hard drive.
 * 
 * Once we run the program, java memory 
 * management will start work:
 *   --> By looking at the components of the 
 *   program, it will store some things into
 *   Stack and others into HEAP
 *   All object always go to HEAP.
 *   
 *   What happen to the class itself.
 *   public class Employee{
 *   int salary;
 *   }
 *   
 *   classes that we use in our program will be loaded to HEAP.
 *   
 *   And anything we create object from classes,
 *   they will be created from class and stored in HEAP.
 */



